<!DOCTYPE html>
<html lang="en">

{% from 'shop/macros/_userbar.jinja' import userbar %}

	<head>
    	<meta charset="utf-8">
    	<meta http-equiv="X-UA-Compatible" content="IE=edge">
    	<meta name="viewport" content="width=device-width, initial-scale=1">
        <title>{% block title %}{% endblock %} | Книга</title>

        <link href="{{ static('css/bootstrap.min.css') }}" rel="stylesheet">
        <link href="{{ static ('css/shop/base.css') }}" rel="stylesheet">
        {% block stylesheet %}{% endblock %}

    </head>
    <body>
        <header>
            <div class="container">
                <div class="footer-main row">
                    <div class="col-md-2">
                        <a href="{{ url('shop:index') }}">
                            <img class="footer-logo" src="{{ static('img/book_logo.jpg') }}" height="100px">
                        </a>
                    </div>

                    <div class="footer-left col-sm-4">
                        <h1 class="page-header"><b>Книга</b></h1>
                        <p class="text-left"><i>Да, вы угадали, мы продаем книги</i></p>
                    </div>

                    <div class="footer-menu col-md-4 col-md-offset-2 row">
                        {{ userbar(request.user) }}

                        <nav class="menu">
                            <ul class="list-inline list-unstyled">
                                <li class="menu-item {% if request.path == url('shop:index') %}active-item{% endif %}"><a href="{{ url('shop:index') }}">Главная</a></li>
                                <li class="menu-item {% if request.path == url('shop:book_list') %}active-item{% endif %}"><a href="{{ url('shop:book_list') }}">Каталог</a></li>
                                <li class="menu-item {% if request.path == url('shop:about') %}active-item{% endif %}"><a href="{{ url('shop:about') }}">О нас</a></li>
                            </ul>
                        </nav>
                    </div>
                </div>
                <div class="clearfix"></div>
                <hr class="footer-end">
            </div>
        </header>

        <main>
            <div class="container">
                {% block content %}
                {% endblock %}
            </div>
        </main>

        <footer>

        </footer>

        <script src="{{ static('js/app.js') }}"></script>

    	<script src="{{ static('js/jquery-3.2.1.min.js') }}"></script>
		<script src="{{ static('js/bootstrap.min.js') }}"></script>
        {% block javascript %}{% endblock %}
    </body>
</html>